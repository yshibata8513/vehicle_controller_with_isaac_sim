# Runtime config for scripts/rl/train_ppo.py with --video.
# Wraps local_paths so the experiment only needs one runtime_ref.
schema_version: 1
paths_ref: configs/runtime/local_paths.yaml

video:
  enabled: true
  length_steps: 200
  interval_iters: 2000
