# Phase 3 lemniscate: legacy --course lemniscate route. Mirrors
# phase3_circle_stage0a scaffolding but selects the lemniscate course YAML so
# radius / target_speed / course_ds invariants stay consistent (PR 3 round-2
# fix F3).
schema_version: 1
kind: rl_train

seed: null
run_name: phase3_lemniscate

vehicle_ref: configs/vehicles/sedan.yaml
dynamics_ref: configs/dynamics/linear_friction_circle_flat.yaml
env_ref: configs/envs/tracking.yaml
course_ref: configs/courses/lemniscate.yaml
agent_ref: configs/agents/rsl_rl/ppo_tracking.yaml
runtime_ref: configs/runtime/local_paths.yaml

overrides:
  env:
    scene:
      num_envs: 128
    spaces:
      action_mode: steering_only
  agent:
    runner:
      max_iterations: 200
      experiment_name: phase3_lemniscate
