[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "vehicle_rl"
version = "0.0.1"
description = "Vehicle motion-control RL on Isaac Sim / Isaac Lab"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
